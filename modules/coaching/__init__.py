"""Coaching — schema-driven prompt building for role-aware coaching.

Uses config/coaching_schema.json to extract exactly the fields LLaMA needs,
apply benchmarks, and assemble a ~800-token prompt.
"""
